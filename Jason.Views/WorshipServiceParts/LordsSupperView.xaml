<UserControl
    x:Class="Jason.Views.WorshipServiceParts.LordsSupperView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel Orientation="Horizontal">
        <TextBlock>
            <TextBlock.Inlines>
                <Run Text="Lord's Supper: "/>
                <Run Text="{Binding Scripture.Book, Mode=OneWay}"/>
                <Run Text="{Binding Scripture.Reference, Mode=OneWay}"/>
            </TextBlock.Inlines>
        </TextBlock>
        <TextBlock Text="("
                   Margin="5,0,0,0"/>
        <TextBlock Text="{Binding Scripture.Translation}"/>
        <TextBlock Text=")"/>
    </StackPanel>
</UserControl>
